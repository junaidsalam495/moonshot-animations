export const metadata = {
  title: "Portfolio - Moonshot Animations",
  description:
    "Explore the portfolio of Moonshot Animations and our past projects.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
