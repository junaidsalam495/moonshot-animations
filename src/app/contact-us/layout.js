export const metadata = {
  title: "Contact Us - Moonshot Animations",
  description:
    "Get in touch with Moonshot Animations for your animation needs.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
